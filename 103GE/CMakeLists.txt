file(GLOB_RECURSE SOURCES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp"
)

set(CORE_SOURCES ${SOURCES} PARENT_SCOPE)
set(CORE_INC "${CMAKE_CURRENT_SOURCE_DIR}/src" PARENT_SCOPE)
